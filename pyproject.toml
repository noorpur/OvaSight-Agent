[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ovasight-agent"
version = "0.1.0"
description = "Human-in-the-loop ovarian tumor ultrasound segmentation agent"
requires-python = ">=3.10"
