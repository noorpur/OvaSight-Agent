# Data folder

Raw medical images are intentionally not included.

Place MMOTU here:

```text
data/raw/MMOTU/
```

Then run:

```bash
python -m ovasight_agent.agent audit --config configs/mmotu_unet_m2.yaml
```
